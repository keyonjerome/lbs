from setuptools import find_packages, setup

setup(
    name="",
    version="0.0.1",
    description="",
    install_requires=[],
    packages=find_packages(where="src"),
    scripts=[],
    zip_safe=False,
)
